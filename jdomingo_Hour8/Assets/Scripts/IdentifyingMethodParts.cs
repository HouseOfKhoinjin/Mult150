using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IdentifyingMethodParts : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //int <--(Method type returned) (method name)--> TakeDamage(int damageAmount) <--(1 int variable/parameter)
        //{
           // int health = 100; <--(variable)
           //return health - damageAmount;
        //}
    }
}
